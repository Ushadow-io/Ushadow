apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ushadow-data
  namespace: ushadow
  labels:
    app: ushadow
    component: data
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
