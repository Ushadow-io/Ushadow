apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ushadow-config
  namespace: ushadow
  labels:
    app: ushadow
    component: config
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
