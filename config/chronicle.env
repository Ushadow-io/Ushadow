# Chronicle Configuration
# This file contains Chronicle-specific settings
# Shared API keys are loaded from .env via OmegaConf interpolation

# ============================================
# Authentication (REQUIRED)
# ============================================
CHRONICLE_ADMIN_EMAIL=admin@chronicle.local
CHRONICLE_ADMIN_PASSWORD=changeme123
CHRONICLE_AUTH_SECRET=your-jwt-secret-must-be-at-least-32-characters-long

# ============================================
# Worker Configuration
# ============================================
ENABLE_SIDECAR_WORKERS=true
RQ_WORKER_COUNT=3

# ============================================
# Service Providers (use shared keys from .env)
# ============================================
# These will use the shared API keys from api_keys namespace
# TRANSCRIPTION_PROVIDER is set based on which key is available
TRANSCRIPTION_PROVIDER=deepgram
LLM_PROVIDER=openai
OPENAI_MODEL=gpt-4o-mini

# ============================================
# Ports (configured per environment)
# ============================================
CHRONICLE_PORT=8080
CHRONICLE_WEBUI_PORT=3080
CHRONICLE_REDIS_DB=1
