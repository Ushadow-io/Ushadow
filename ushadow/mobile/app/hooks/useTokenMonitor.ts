/**
 * useTokenMonitor — Re-export shim.
 *
 * Source of truth has moved to @ushadow/mobile-core/auth.
 * This file re-exports so existing imports keep working.
 */

export { useTokenMonitor } from '../../../../packages/mobile-core/auth/useTokenMonitor';
