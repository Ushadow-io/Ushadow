# nar8 — Fastlane configuration
#
# All lanes are provided by the shared SharedFastfile.
# App-specific config lives in ../build.json and ../eas.json.

import "../../../packages/mobile-build/fastlane/SharedFastfile"
